"""
This task demonstrates the use of numpy
"""
import numpy as np


def sort_array(arr):
    return np.sort(arr)


def find_min_max(arr):
    return f"here is the min: {np.min(arr)}, here is the max: {np.max(arr)}"
